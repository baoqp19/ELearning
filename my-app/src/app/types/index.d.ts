

type IActiveLinkProps = {
    url: string;
    children: React.ReactNode;
};


type IMenuItem = {
    url: string;
    title: string;
    icon: React.ReactNode;
};


export { IActiveLinkProps, IMenuItem };